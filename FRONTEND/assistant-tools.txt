nume functie: create_appointment

descriere functie: Creează o programare nouă în calendar. Necesită nume, serviciu, dată (YYYY-MM-DD) și oră (HH:MM).

json schema functie:

{
  "type": "object",
  "properties": {
    "notes": {
      "description": "Note opționale (ex: 'prima vizită', 'pacient anxios'). OPȚIONAL.",
      "type": "string"
    },
    "patient_name": {
      "description": "Numele complet al pacientului (ex: Ion Popescu). OBLIGATORIU.",
      "type": "string"
    },
    "service_type": {
      "description": "Tipul de serviciu dentar solicitat. OBLIGATORIU.",
      "type": "string",
      "enum": [
        "Consultație",
        "Igienizare",
        "Extracție",
        "Tratament canal",
        "Implant",
        "Proteză",
        "Ortodonție",
        "Albire",
        "Detartraj",
        "Plombă"
      ]
    },
    "preferred_date": {
      "description": "Data programării în format YYYY-MM-DD (ex: 2025-11-06). Calculează corect pentru 'mâine', 'luni', etc. OBLIGATORIU.",
      "type": "string"
    },
    "preferred_time": {
      "description": "Ora programării în format HH:MM (ex: 14:00, 09:30). Doar ore întregi sau la jumătate. Program: 10:00-20:00. OBLIGATORIU.",
      "type": "string"
    }
  },
  "required": [
    "patient_name",
    "service_type",
    "preferred_date",
    "preferred_time"
  ]
}

###

nume functie: check_availability

descriere functie: Verifică orele disponibile pentru o anumită zi. Folosește DOAR dacă pacientul întreabă "ce aveți liber?".

json schema functie:

{
  "type": "object",
  "properties": {
    "date": {
      "description": "Data pentru care verifici disponibilitatea, în format YYYY-MM-DD (ex: 2025-10-30). Dacă pacientul spune 'mâine', 'vineri', calculează data corectă în funcție de ziua curentă.",
      "type": "string"
    }
  },
  "required": [
    "date"
  ]
}

###

nume functie: cancel_appointment

descriere functie: Anulează o programare existentă. Necesită numele pacientului și numărul de telefon.

json schema functie:

{
  "type": "object",
  "properties": {
    "patient_name": {
      "description": "Numele complet al pacientului (ex: Ion Popescu). Trebuie să fie exact cum a fost introdus la programare.",
      "type": "string"
    },
    "phone_number": {
      "description": "Numărul de telefon al pacientului (ex: 0722123456). Folosit pentru identificare precisă a programării.",
      "type": "string"
    }
  },
  "required": [
    "patient_name",
    "phone_number"
  ]
}
